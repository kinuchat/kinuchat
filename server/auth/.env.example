# MeshLink Auth Service Configuration

# Database connection
DATABASE_URL=postgres://meshlink:meshlink@localhost:5432/meshlink_auth

# Server address
LISTEN_ADDR=0.0.0.0:3000

# WebAuthn configuration
WEBAUTHN_RP_ID=meshlink.app
WEBAUTHN_RP_ORIGIN=https://meshlink.app

# JWT secret (change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Email encryption key (32 bytes as 64 hex chars)
# Generate with: openssl rand -hex 32
EMAIL_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Logging level
RUST_LOG=meshlink_auth=debug,tower_http=debug
